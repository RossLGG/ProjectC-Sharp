@model List<VentasOrganosCortes.Models.ViewModels.ListServicioViewModel>
@{
    ViewBag.Title = "Mis datos";
}

<h2>@ViewBag.Title</h2>

<div class="row">
    <div class="col-md-12">
        <table class="table">
            <tr>
                <th>ID</th>
                <th>Precio</th>
                <th>ID Vendedor</th>
                <th></th>
            </tr>
            @foreach (var oElemento in Model)
            {
                <tr>
                    <td>@oElemento.Id</td>
                    <td>@oElemento.Precio</td>
                    <td>@oElemento.Id_Vend</td>
                    <td>
                        <a class="btn btn-link" href="~/Servicio/Editar/@oElemento.Id">Editar</a>
                        <a class="btn btn-link" href="~/Servicio/Eliminar/@oElemento.Id">Eliminar</a>
                    </td>
                </tr>
            }
        </table>
        <div class="row" style="text-align:right;padding:5px;">
            <a href="~/Servicio/Nuevo" class="btn btn-primary">Nuevo</a>
        </div>
    </div>
</div>
